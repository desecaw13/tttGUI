cButton:
 --custom tkinter Button
 --defined in flowchart

root:
 button singleplayer
 button multiplayer
 button exit

start:
 entry size --max 9, min 3
 button play
 button back

game:
 list board --len(board)=len(board[n])=size.get()
 for n in board: for b in n: cButton

win:
 label winner
 button back -- goes to root
